# =========================================================
# MongoDB Atlas Connection
# =========================================================
# Замініть <user>, <password>, <cluster> на ваші дані з MongoDB Atlas.
MONGO_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/chatdb?retryWrites=true&w=majority&appName=ChatAppCluster


# =========================================================
# Server Configuration 
# =========================================================
# Порт, на якому буде працювати Express.js
PORT=5000

# URL клієнтського React-додатку (для CORS та Socket.IO origin)
# Локально: http://localhost:3000
# На продакшені: https://your-frontend-app.vercel.app
CLIENT_URL=http://localhost:3000


# =========================================================
# Google OAuth (Passport.js)
# =========================================================
# Отримайте ці ключі з Google Cloud Console.
GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID
GOOGLE_CLIENT_SECRET=YOUR_GOOGLE_CLIENT_SECRET
# URL, на який Google повертає користувача після логіну (має співпадати з налаштуваннями Google Console)
GOOGLE_CALLBACK_URL=http://localhost:3000/auth/google/callback

# Секретний ключ для шифрування сесій Express (мінімум 32 символи)
SESSION_SECRET=aVerySecureSecretForExpressSessions